# pkgwatch - Stay up to date.
**pkgwatch** is a CLI tool built with JavaScript to tell you if you have any outdated dependencies in your `package.json` file.

To get started run:

```sh
npx pkgwatch@latest
```
